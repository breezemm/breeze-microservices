services:
  kafka-go:
    build:
      context: .
      dockerfile: ./apps/kafka-go/Dockerfile
    ports:
      - 8005:3000
    volumes:
      - ./apps/kafka-go:/app
    depends_on:
      - kafka
    networks:
      - dev
