{
    "name": "gateway",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "php artisan serve --port=8000",
        "queue": "sudo supervisord -c supervisor.conf",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.6.7",
        "chokidar": "^3.5.3",
        "laravel-vite-plugin": "^1.0.1",
        "vite": "^5.0.2"
    }
}
