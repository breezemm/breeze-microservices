{
    "name": "gateway",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "php artisan serve --port=8000",
        "queue": "sudo supervisord -c supervisor.conf",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.8.1",
        "vite": "^5.0.11"
    }
}
